Analisi del problema


Gestione libri:

funzione per che richiede permette di inserire un nuovo libro. Salva in un dizionario
funzione che prende come parametro il dizionario e lo salva su un file (json)


Gestione utenti:

funzione che permette di inserire un nuovo utente. Salva in un dizionario
funzione che prende come parametro il dizionario e lo salva su un file (json)


Prestiti e restituzioni

Un ciclo che permette all’utente di continuare a prendere in prestito libri fin quando vuole. Gestione di eventuali errori per esempio se l’utente prova a prendere in prestito un libro che non è disponibile.
Salva i prestati su un file
modifica del file dei libri per la disponibilità di copie

Main

Menù principale
